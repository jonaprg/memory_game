export class Juez {
    constructor() {
        this.juzgar();
        this.initialFlip();
    }
    juzgar(fixa1, fixa2) {
        // si coinciden, animación que muestra al usuario que asertó
        // si no coinciden, girar dos fixas otra vez
    }
    initialFlip(timepo, ...fixa) {
        // mostrar a todas la fixas por unos segundos antes de iniciar partida
    }
} 